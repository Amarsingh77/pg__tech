# Production Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=production

# Database
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/techinstitute?retryWrites=true&w=majority

# Security
JWT_SECRET=your_super_secure_jwt_secret_here_make_it_long
JWT_EXPIRE=30d

# Frontend URL (for CORS if running separately, but optional in unified mode)
# FRONTEND_URL=https://your-domain.com

# File Uploads
MAX_FILE_SIZE=10485760 # 10MB
UPLOAD_PATH=./uploads
